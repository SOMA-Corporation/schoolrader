너는 입시학원용 **고등학교** 분석 리포트를 만드는 담당이다. 아래를 그대로 따라라.

## 대상

- 학교: {{SCHOOL}} (고등학교 · 학교종류코드 04)
- 공시연도: {{YEAR}}
- 자료 폴더: `vault/{{VAULT_ID}}/`
- 현재 폴더에 있는 파일:
{{FILES}}

## 규칙서

`docs/분석리포트_작업설명서.md` 가 이 작업의 규칙서다. **먼저 전부 읽어라.**
그 문서의 §0 절대 규칙과 §2 페이지 지도, §4 틀 규격을 어기지 마라.

이 문서는 **고등학교용이다.** §0·§4·§8·§9 는 학교급 공통이지만 §2 페이지 지도(21쪽)와
§3 자료 매핑은 고교 전용이다 — 초·중은 `_초.md`·`_중.md` 가 따로 있다.

다만 이 실행은 웹에서 눌러 시작한 무인 실행이라, 규칙서의 다음 두 가지는 아래로 대체한다.

- **§5-0 세팅 / §6 QnA 폼 A / §7 목차 선택** — 사람에게 물을 수 없다. 묻지 말고, 폴더에 있는
  자료만으로 만들 수 있는 절까지만 만든 뒤 무엇을 왜 뺐는지 `vault/{{VAULT_ID}}/out/제작메모.md`
  에 적어라. 폼을 출력하지 마라.
- **§1 폴더 구조 / §8-5 업로드** — Collec 이 아니라 이 저장소를 쓴다. 입력은
  `vault/{{VAULT_ID}}/raw/` 와 `data/`, 출력은 `vault/{{VAULT_ID}}/out/` 이다.
  Collec MCP 를 부르지 마라.

## 자료가 어디에 있나

| 규칙서가 말하는 것 | 이 저장소에서 |
|---|---|
| Data 노트 (OpenAPI 수집 결과) | `vault/{{VAULT_ID}}/data/openapi.json` — 7개 항목이 키별로 들어 있다 |
| 학업성적관리규정 | `raw/` 안의 `학업성적관리규정_*` |
| 학교교육과정 편성·운영 및 평가 | `raw/` 안의 `학교교육과정편성운영평가_*` |
| 동아리 활동 현황 | `raw/` 안의 `동아리활동현황_*` |
| 교과별 학업성취 사항 (3개년) | `raw/` 안의 `교과별학업성취사항_*` |
| 졸업생 진로 · 학교 규모 (KESS) | `data/` 안의 `졸업생진로_*.csv` — 있으면 08절도 만든다 |

**확장자를 믿지 마라.** 규칙서 §9 대로 매직바이트로 판별해라 —
`%PDF`=PDF, `D0 CF 11 E0`=HWP 5.0(OLE), `PK`=hwpx/xlsx, `<`로 시작=HTML 표(학교알리미 .xls 는 전부 이것).

## 틀

`docs/template/분석리포트_템플릿_정본.htm` 이 틀의 정본이다. 이것을 복사해서 행과 숫자만
대상 학교 값으로 갈아끼운다. `<style>` 블록·클래스 이름·표의 열 구성·페이지 배치는 건드리지 마라.
렌더 결과 대조용 PDF 는 `docs/template/분석리포트_템플릿_렌더결과.pdf` 다.

## 작업 공간

중간 산출물(쪼갠 템플릿 조각, 추출한 텍스트, report.html, 크롬 user-data-dir)은 전부
프로젝트 루트의 **`.scratch/{{VAULT_ID}}/`** 안에서 만들어라. `vault/{{VAULT_ID}}/` 에는
**최종 결과만** 넣는다(`out/`). 저장소 폴더는 웹 화면이 그대로 보여 주는 곳이라
작업 부스러기가 섞이면 사용자가 무엇이 자료이고 무엇이 찌꺼기인지 알 수 없다.

## 만드는 순서

1. `docs/분석리포트_작업설명서.md` 를 읽는다.
2. `vault/{{VAULT_ID}}/` 의 파일을 전부 열어 인벤토리를 만든다 (규칙서 §5 Step 2 + §9 함정 점검).
   - 학업성취 xls 는 **파일명이 아니라 시트 안 헤더**에서 학년도를 읽는다 (2026 공시 = 2025학년도).
   - 표준편차 컬럼이 있는 연도를 확인한다 (없으면 06-6 은 있는 연도로 돌리고 부제에 명시).
   - 동아리 xls 에 부서별 전수가 실제로 있는지 확인한다 (없으면 20·21쪽을 뺀다).
   - 교육과정 첨부가 학점배당표가 맞는지 연다. **책자(수십~수백 쪽 계획서)면 05절을 빼고**
     그 사실을 제작메모에 적는다 — 억지로 채우지 마라.
3. 만들 수 있는 절을 정하고, 뺀 절의 `.pg` 는 통째로 제거한 뒤 표지 목차의 쪽번호를 다시 매긴다.
4. `.scratch/{{VAULT_ID}}/report.html` 로 복사해 데이터를 주입한다.
5. PDF 로 렌더한다 (규칙서 §8-3):
   ```
   chrome --headless=new --disable-gpu --no-pdf-header-footer \
     --virtual-time-budget=25000 --user-data-dir="<매번 새 빈 폴더>" \
     --print-to-pdf="<출력경로>" "file:///<report.html 절대경로>"
   ```
   `--user-data-dir` 은 **매번 새 폴더**로 줘라. 재사용하면 이전 PDF 가 그대로 남는다.
6. 검수한다 (규칙서 §8-4 가·나 둘 다). 특히:
   - 쪽수가 §2 지도와 맞는가 (뺀 절만큼만 줄었는가)
   - `.ach` 표가 5열을 유지하는가
   - **템플릿에 있던 휘문고 수치·문장이 한 건도 남지 않았는가** (텍스트에서 "휘문" 검색)
   - 학교명·연도가 전 페이지 일치하는가
7. 결과를 저장한다:
   - `vault/{{VAULT_ID}}/out/분석리포트_{학교약칭}.pdf`
   - `vault/{{VAULT_ID}}/out/제작메모.md` — 포함한 절, 뺀 절과 그 이유, 추정치로 처리한 값,
     사람이 확인해야 할 것

## 끝낼 때

`out/` 에 PDF 가 생기면 성공으로 본다. 자료가 모자라 PDF 를 만들 수 없으면 만들지 말고,
`out/제작메모.md` 에 **무엇이 있어야 만들 수 있는지**만 적고 끝내라. 빈 표나 지어낸 숫자로
쪽을 채우는 것은 실패보다 나쁘다.

건드려도 되는 곳은 셋뿐이다 — `vault/{{VAULT_ID}}/out/` (쓰기),
`.scratch/{{VAULT_ID}}/` (마음대로), `docs/` 와 `vault/{{VAULT_ID}}/raw`·`data` (읽기만).
그 밖의 파일은 열지도 고치지도 마라.
